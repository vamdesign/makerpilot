/** Gray outline — tappable cards/rows (choose-listings style). */
export const cardBorderTouchable = 'border-2 border-[#E5E7EB]';

/** Teal outline — display/form cards (inventory style). */
export const cardBorderStatic = 'border-2 border-[#1A9E8F]';
